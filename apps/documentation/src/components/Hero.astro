---
import phycomLogo from "../../public/phycom_logo.svg";
import Container from "./Container.astro";
import { Image } from "astro:assets";
import { ChevronDown, Code, Gauge } from "@lucide/astro";
---

<section class="flex items-center justify-center py-10 px-4">
  <Container>
    <div class="relative z-10">
      <Image
        src={phycomLogo}
        alt="Physical Computing Logo"
        class="mx-auto w-60 h-auto sm:w-80 md:w-96 mb-6"
      />
    </div>
    <div
      class="relative z-10 flex flex-col items-center gap-4 sm:gap-6 animate-fade-in-up animation-delay-200"
    >
      <h1
        class="text-xl sm:text-3xl md:text-4xl font-bold text-center drop-shadow-lg"
      >
        Helmet Detection System <br />for Automatic Door Access
      </h1>
      <p class="max-w-2xl text-base sm:text-lg text-center text-zinc-700">
        ระบบตรวจจับหมวกกันน็อคอัตโนมัติสำหรับควบคุมการเปิด-ปิดประตูไม้กั้น
        โดยใช้กล้องและ Raspberry Pi
        เป็นอุปกรณ์หลักในการตรวจจับผู้ขับขี่มอเตอร์ไซค์ที่สวมหมวกกันน็อคหรือไม่
        ระบบนี้สามารถประมวลผลภาพแบบ real-time
        และสั่งเปิดไม้กั้นได้ทันทีหากตรวจพบว่าผู้ขับสวมหมวกอย่างถูกต้อง
        ทั้งยังสามารถเก็บข้อมูลและแสดงผลสถิติผ่าน Dashboard ออนไลน์
      </p>
      <div class="flex justify-center flex-wrap gap-4 w-full sm:w-auto mb-10">
        <a
          href="https://github.com/Phoz07/physical-computing"
          target="_blank"
          class="uppercase bg-amber-400 hover:bg-amber-300 p-2 h-fit rounded-full font-bold px-4 flex gap-2 items-center"
          ><Code /> ซอร์สโค้ด</a
        >
        <a
          href="https://phycom.suwizx.dev/"
          target="_blank"
          class="uppercase bg-zinc-100 hover:bg-zinc-200 p-2 h-fit rounded-full font-bold px-4 flex gap-2 items-center"
          ><Gauge />ตัวอย่างแผงควบคุม</a
        >
      </div>
      <a
        href="#abstract"
        class="uppercase bg-amber-50 text-amber-500 px-2 text-sm h-fit rounded-full flex gap-2 items-center animate-bounce"
        ><ChevronDown stroke-width={1.4} />เลื่อนลงเพื่อดูรายละเอียด</a
      >
    </div>
  </Container>
</section>
